# Fix Baseband
allow priv_app baseband_prop:file read;
get_prop(priv_app, baseband_prop)

allow priv_app unlabeled:dir { search read open getattr search };
allow priv_app system_data_file:dir { read open };